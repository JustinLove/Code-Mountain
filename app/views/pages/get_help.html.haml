#get_help
  = form_tag '/help_confirm', :class => 'help' do
    .help_title Help, I'm Stuck
    .help_detail
      .question
        Where did you get stuck?
      = text_area :help, :question, :rows => 5, :cols => 50, :value => "I'm stuck on Task 3: Rails for Zombies.\n\nZombie.find(3).graveyard = 'Benny Hills Memorial'\n"
      .send_to
        Send this to:
        .group
          = check_box 'help', 'friends',  :checked => 'checked'
          Your Friends
        .group
          = check_box 'help', 'experts'
          Expert Climbers
      .submit
        = image_submit_tag '/images/get_help_button.png'

